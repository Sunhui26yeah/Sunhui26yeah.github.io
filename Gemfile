source 'https://rubygems.org'

# Local dev/build only. GitHub Pages builds the live site with its own
# managed Jekyll pipeline, independent of this Gemfile.
gem 'jekyll'
gem 'webrick'

group :jekyll_plugins do
  gem 'jekyll-feed'
  gem 'jekyll-sitemap'
  gem 'jekyll-redirect-from'
  gem 'jemoji'
  gem 'jekyll-paginate'
end
